<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup><OutputType>Exe</OutputType><TargetFramework>net10.0</TargetFramework><ImplicitUsings>enable</ImplicitUsings><Nullable>enable</Nullable></PropertyGroup>
  <ItemGroup><ProjectReference Include="../../../src/Koh.Verify/Koh.Verify.csproj" /></ItemGroup>
  <!-- The shared renderer compiles into the verifier itself (against PhaseSweepCheck's managed
       Surface stand-in) so the all-phases sweep exercises the exact source the ROMs are built from. -->
  <ItemGroup>
    <Compile Include="../shared/FixedMath.cs" Link="Shared/FixedMath.cs" />
    <Compile Include="../shared/CubeRenderer.cs" Link="Shared/CubeRenderer.cs" />
  </ItemGroup>
</Project>
